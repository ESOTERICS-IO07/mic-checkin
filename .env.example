# Supabase (safe to expose — anon key is RLS-gated)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Optional: Google OAuth is structurally prepared but not required for Phase A.
# Enable the Google provider in the Supabase dashboard only when you are ready.
# NEXT_PUBLIC_SUPABASE_GOOGLE_AUTH_ENABLED=false
